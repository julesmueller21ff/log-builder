version: '3.8'
services:
  app:
    build: .
    ports:
      - "5275:5205"
    environment:
      - NODE_ENV=production
    volumes:
      - ./data:/app/data
  db:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: log_builder
      POSTGRES_PASSWORD: changeme
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
